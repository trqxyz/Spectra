name: Release

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push'
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build shaded JAR
        run: ./gradlew shadowJar

      - name: Resolve shaded JAR path
        id: shaded-jar
        run: echo "path=$(./gradlew -q printShadowJarPath)" >> "$GITHUB_OUTPUT"

      - name: Build unbundled JAR
        run: ./gradlew shadowJar -PshadePE=false

      - name: Resolve unbundled JAR path
        id: unbundled-jar
        run: echo "path=$(./gradlew -q printShadowJarPath -PshadePE=false)" >> "$GITHUB_OUTPUT"

      - name: Generate tag
        id: tag
        run: echo "tag=v$(date -u +%Y.%m.%d)-${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "${{ steps.tag.outputs.tag }}"
          name: "${{ steps.tag.outputs.tag }}"
          body: |
            Automatic release from commit ${{ github.event.workflow_run.head_sha }}.
          files: |
            ${{ steps.shaded-jar.outputs.path }}
            ${{ steps.unbundled-jar.outputs.path }}
